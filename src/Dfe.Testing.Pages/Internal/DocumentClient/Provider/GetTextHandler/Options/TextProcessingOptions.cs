namespace Dfe.Testing.Pages.Internal.DocumentClient.Provider.GetTextHandler.Options;
// TODO expand to replacable options - compiled regex at startup?
public sealed class TextProcessingOptions
{
    public bool Trim { get; set; } = false;
}
